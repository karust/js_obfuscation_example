# js_obfuscation_example

- Obfuscate `input.js`:

```
node obfuscate.js
```

See results in `output.js`

- Deobfuscate `output.js`:

Change function name here `let functionName = "Up";` first.

```
node deobfuscate.js
```

See results in `input.js`
